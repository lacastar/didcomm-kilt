<template>
  <router-view />
</template>

<script setup>

//import * as Kilt from '@kiltprotocol/sdk-js'

//import { onMounted, inject } from 'vue'
//import { useKiltStore } from '@/store/kilt'



//const Kilt = inject('kilt')
//const polkadotjs = inject('polkadotjs')
//const kiltStore = useKiltStore()
/*
onMounted(async () => {
  await Kilt.connect(import.meta.env.VITE_KILT_WSS_ADDRESS)
  const api = Kilt.ConfigService.get('api')
  //kiltStore.init(api, Kilt, polkadotjs)
  kiltStore.init(api)
}
)*/
</script>
